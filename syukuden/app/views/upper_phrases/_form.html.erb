<%= form_for(@upper_phrase) do |f| %>
  <% if @upper_phrase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@upper_phrase.errors.count, "error") %> prohibited this upper_phrase from being saved:</h2>

      <ul>
      <% @upper_phrase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :available %><br />
    <%= f.check_box :available %>
  </div>
  <div class="field">
    <%= f.label :deleted %><br />
    <%= f.check_box :deleted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
