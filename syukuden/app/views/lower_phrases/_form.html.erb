<%= form_for(@lower_phrase) do |f| %>
  <% if @lower_phrase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lower_phrase.errors.count, "error") %> prohibited this lower_phrase from being saved:</h2>

      <ul>
      <% @lower_phrase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :available %><br />
    <%= f.check_box :available %>
  </div>
  <div class="field">
    <%= f.label :deleted %><br />
    <%= f.check_box :deleted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
